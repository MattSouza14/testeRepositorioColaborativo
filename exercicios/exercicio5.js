// Leia base e altura de um triângulo e calcule sua área (Área do triangulo: (b*h)/2)

let base = Number(prompt("Base: "))
let altura = Number(prompt("Altura: "))

console.log(`A área do triângulo é ${(base*altura)/2}`)